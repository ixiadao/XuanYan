导入 std::获取必需程序参数;
导入 std::读取文本文件;
导入 std::文本替换;
导入 std::写入文本文件;

函数 主函数() -> 结果<字符串, 字符串> {
    令 输入路径 = 获取必需程序参数(0, "缺少输入路径")?;
    令 输出路径 = 获取必需程序参数(1, "缺少输出路径")?;
    令 原片段 = 获取必需程序参数(2, "缺少原片段")?;
    令 新片段 = 获取必需程序参数(3, "缺少新片段")?;
    令 输入 = 读取文本文件(输入路径)?;
    令 输出 = 文本替换(输入, 原片段, 新片段)?;
    写入文本文件(输出路径, 输出)?;
    结果::成功(输出路径)
}
