导入 std::读取文本输入;
导入 std::文本分行;
导入 std::文本连接;

函数 汇总(文本: 字符串) -> 结果<整数, 字符串> {
    变量 总和 = 0;
    遍历 行 在 文本分行(文本) {
        总和 = 总和 + 整数::从文本(行)?;
    }
    结果::成功(总和)
}

函数 主函数() -> 结果<字符串, 字符串> {
    令 文本 = 读取文本输入(0)?;
    令 总和 = 汇总(文本)?;
    结果::成功(文本连接("总和：", 总和.到文本()))
}
