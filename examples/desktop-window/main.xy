导入 std::显示桌面确认框;
导入 std::运行桌面窗口;

函数 创建空值() -> 空 {}

函数 主函数() -> 结果<空, 字符串> {
    令 打开窗口 = 显示桌面确认框("玄言桌面", "是否打开空白原生窗口？")?;
    如果 打开窗口 {
        运行桌面窗口("玄言桌面窗口", 720, 480)
    } 否则 {
        结果::成功(创建空值())
    }
}
