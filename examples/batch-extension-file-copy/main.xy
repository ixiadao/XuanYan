导入 std::创建目录;
导入 std::获取必需程序参数;
导入 std::列表;
导入 std::读取目录文件;
导入 std::路径连接;
导入 std::路径扩展名是;
导入 std::路径文件名;
导入 std::复制文件;

函数 准备批量复制(输入目录: 借用<字符串>, 输出目录: 借用<字符串>) -> 结果<列表<字符串>, 字符串> {
    令 输入路径列表 = 读取目录文件(输入目录)?;
    创建目录(输出目录)?;
    结果::成功(输入路径列表)
}

函数 批量复制指定扩展名(输入目录: 字符串, 输出目录: 字符串, 目标扩展名: 字符串) -> 结果<字符串, 字符串> {
    遍历 输入路径 在 准备批量复制(输入目录, 输出目录)? {
        如果 非 路径扩展名是(输入路径, 目标扩展名) {
            继续;
        }
        令 文件名 = 路径文件名(输入路径)?;
        令 输出路径 = 路径连接(输出目录, 文件名);
        复制文件(输入路径, 输出路径)?;
    }
    结果::成功(输出目录)
}

函数 主函数() -> 结果<字符串, 字符串> {
    令 输入目录 = 获取必需程序参数(0, "缺少输入目录")?;
    令 输出目录 = 获取必需程序参数(1, "缺少输出目录")?;
    令 目标扩展名 = 获取必需程序参数(2, "缺少目标扩展名")?;
    批量复制指定扩展名(输入目录, 输出目录, 目标扩展名)
}
