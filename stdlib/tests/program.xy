导入 src::程序与控制台::获取必需程序参数;

公开 函数 程序参数行为正确() -> 布尔 {
    令 负下标正确 = 匹配 获取必需程序参数(-1, "负下标") {
        结果::成功(_) => 假,
        结果::失败(错误) => 错误 == "负下标",
    };
    令 越界正确 = 匹配 获取必需程序参数(999999, "越界") {
        结果::成功(_) => 假,
        结果::失败(错误) => 错误 == "越界",
    };
    负下标正确 且 越界正确
}
