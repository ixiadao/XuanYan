导入 src::列表::列表;
导入 src::列表::列表创建;
导入 src::列表::列表追加;
导入 src::列表::列表长度;
导入 src::列表::列表反转;
导入 src::列表::列表清空;
导入 src::列表::列表是空;
导入 src::列表::列表连接;
导入 src::列表::列表取出;
导入 src::列表::列表取出末项;
导入 src::列表::列表取出指定项;

函数 创建待遍历列表() -> 列表<整数> {
    变量 数字: 列表<整数> = 列表创建();
    列表追加(数字, 20);
    列表追加(数字, 22);
    数字
}

公开 函数 列表行为正确() -> 布尔 {
    变量 空数字: 列表<整数> = 列表创建();
    变量 空文本: 列表<字符串> = 列表创建();
    如果 非 列表是空(空数字) 或 非 列表是空(空文本) {
        返回 假;
    }
    变量 双空左: 列表<整数> = 列表创建();
    变量 双空右: 列表<整数> = 列表创建();
    令 双空连接 = 列表连接(双空左, 双空右);
    如果 非 列表是空(双空连接) {
        返回 假;
    }
    变量 右侧文本: 列表<字符串> = 列表创建();
    列表追加(右侧文本, "玄");
    列表追加(右侧文本, "言");
    变量 左侧空文本: 列表<字符串> = 列表创建();
    变量 左空连接 = 列表连接(左侧空文本, 右侧文本);
    令 左空首项正确 = 匹配 列表取出(左空连接) {
        可选::有值(值) => 值 == "玄",
        可选::空值 => 假,
    };
    令 左空末项正确 = 匹配 列表取出(左空连接) {
        可选::有值(值) => 值 == "言",
        可选::空值 => 假,
    };
    如果 非 左空首项正确 或 非 左空末项正确 或 非 列表是空(左空连接) {
        返回 假;
    }
    变量 左侧数字: 列表<整数> = 列表创建();
    列表追加(左侧数字, 42);
    变量 右侧空数字: 列表<整数> = 列表创建();
    变量 右空连接 = 列表连接(左侧数字, 右侧空数字);
    令 右空项正确 = 匹配 列表取出(右空连接) {
        可选::有值(值) => 值 == 42,
        可选::空值 => 假,
    };
    如果 非 右空项正确 或 非 列表是空(右空连接) {
        返回 假;
    }
    变量 左侧多项: 列表<整数> = 列表创建();
    列表追加(左侧多项, 20);
    列表追加(左侧多项, 22);
    变量 右侧多项: 列表<整数> = 列表创建();
    列表追加(右侧多项, 24);
    列表追加(右侧多项, 22);
    变量 多项连接 = 列表连接(左侧多项, 右侧多项);
    如果 列表长度(多项连接) != 4 {
        返回 假;
    }
    令 多项首项正确 = 匹配 列表取出(多项连接) {
        可选::有值(值) => 值 == 20,
        可选::空值 => 假,
    };
    令 多项次项正确 = 匹配 列表取出(多项连接) {
        可选::有值(值) => 值 == 22,
        可选::空值 => 假,
    };
    令 多项第三项正确 = 匹配 列表取出(多项连接) {
        可选::有值(值) => 值 == 24,
        可选::空值 => 假,
    };
    令 多项末项正确 = 匹配 列表取出(多项连接) {
        可选::有值(值) => 值 == 22,
        可选::空值 => 假,
    };
    如果 非 多项首项正确 或 非 多项次项正确 或 非 多项第三项正确 或 非 多项末项正确 或 非 列表是空(多项连接) {
        返回 假;
    }
    列表反转(空数字);
    如果 非 列表是空(空数字) 或 列表长度(空数字) != 0 {
        返回 假;
    }
    列表追加(空文本, "玄言");
    列表反转(空文本);
    如果 列表是空(空文本) 或 列表长度(空文本) != 1 {
        返回 假;
    }
    变量 清空数字: 列表<整数> = 列表创建();
    列表追加(清空数字, 20);
    列表追加(清空数字, 22);
    列表清空(清空数字);
    如果 非 列表是空(清空数字) 或 列表长度(清空数字) != 0 {
        返回 假;
    }
    列表清空(清空数字);
    如果 非 列表是空(清空数字) {
        返回 假;
    }
    令 文本指定项正确 = 匹配 列表取出指定项(空文本, 0) {
        可选::有值(值) => 值 == "玄言",
        可选::空值 => 假,
    };
    令 空文本指定项为空 = 匹配 列表取出指定项(空文本, 0) {
        可选::有值(_) => 假,
        可选::空值 => 真,
    };
    如果 非 文本指定项正确 或 非 空文本指定项为空 或 非 列表是空(空文本) {
        返回 假;
    }
    变量 反转数字: 列表<整数> = 列表创建();
    列表追加(反转数字, 20);
    列表追加(反转数字, 22);
    列表追加(反转数字, 24);
    列表追加(反转数字, 22);
    列表反转(反转数字);
    如果 列表长度(反转数字) != 4 {
        返回 假;
    }
    令 反转首项正确 = 匹配 列表取出(反转数字) {
        可选::有值(值) => 值 == 22,
        可选::空值 => 假,
    };
    令 反转次项正确 = 匹配 列表取出(反转数字) {
        可选::有值(值) => 值 == 24,
        可选::空值 => 假,
    };
    令 反转第三项正确 = 匹配 列表取出(反转数字) {
        可选::有值(值) => 值 == 22,
        可选::空值 => 假,
    };
    令 反转末项正确 = 匹配 列表取出(反转数字) {
        可选::有值(值) => 值 == 20,
        可选::空值 => 假,
    };
    如果 非 反转首项正确 或 非 反转次项正确 或 非 反转第三项正确 或 非 反转末项正确 或 非 列表是空(反转数字) 或 列表长度(反转数字) != 0 {
        返回 假;
    }
    变量 指定数字: 列表<整数> = 列表创建();
    列表追加(指定数字, 20);
    列表追加(指定数字, 22);
    列表追加(指定数字, 24);
    列表追加(指定数字, 26);
    令 负下标为空 = 匹配 列表取出指定项(指定数字, -1) {
        可选::有值(_) => 假,
        可选::空值 => 真,
    };
    令 越界为空 = 匹配 列表取出指定项(指定数字, 4) {
        可选::有值(_) => 假,
        可选::空值 => 真,
    };
    令 中间项正确 = 匹配 列表取出指定项(指定数字, 1) {
        可选::有值(值) => 值 == 22,
        可选::空值 => 假,
    };
    令 首项指定正确 = 匹配 列表取出指定项(指定数字, 0) {
        可选::有值(值) => 值 == 20,
        可选::空值 => 假,
    };
    令 末项指定正确 = 匹配 列表取出指定项(指定数字, 1) {
        可选::有值(值) => 值 == 26,
        可选::空值 => 假,
    };
    令 剩余项正确 = 匹配 列表取出(指定数字) {
        可选::有值(值) => 值 == 24,
        可选::空值 => 假,
    };
    如果 非 负下标为空 或 非 越界为空 或 非 中间项正确 或 非 首项指定正确 或 非 末项指定正确 或 非 剩余项正确 或 非 列表是空(指定数字) {
        返回 假;
    }
    变量 数字: 列表<整数> = 列表创建();
    列表追加(数字, 20);
    列表追加(数字, 22);
    列表追加(数字, 24);
    列表追加(数字, 26);
    如果 列表是空(数字) 或 列表长度(数字) != 4 {
        返回 假;
    }
    令 末项正确 = 匹配 列表取出末项(数字) {
        可选::有值(值) => 值 == 26,
        可选::空值 => 假,
    };
    如果 非 末项正确 或 列表长度(数字) != 3 {
        返回 假;
    }
    令 次末项正确 = 匹配 列表取出末项(数字) {
        可选::有值(值) => 值 == 24,
        可选::空值 => 假,
    };
    如果 非 次末项正确 或 列表长度(数字) != 2 {
        返回 假;
    }
    令 首项正确 = 匹配 列表取出(数字) {
        可选::有值(值) => 值 == 20,
        可选::空值 => 假,
    };
    如果 非 首项正确 或 列表长度(数字) != 1 {
        返回 假;
    }
    令 最后项正确 = 匹配 列表取出末项(数字) {
        可选::有值(值) => 值 == 22,
        可选::空值 => 假,
    };
    如果 非 最后项正确 或 列表长度(数字) != 0 {
        返回 假;
    }
    令 末项空列表 = 匹配 列表取出末项(数字) {
        可选::有值(_) => 假,
        可选::空值 => 真,
    };
    令 首项空列表 = 匹配 列表取出(数字) {
        可选::有值(_) => 假,
        可选::空值 => 真,
    };
    如果 非 末项空列表 或 非 首项空列表 或 非 列表是空(数字) 或 列表长度(数字) != 0 {
        返回 假;
    }
    变量 文本末项: 列表<字符串> = 列表创建();
    列表追加(文本末项, "玄");
    列表追加(文本末项, "言");
    令 文本末项正确 = 匹配 列表取出末项(文本末项) {
        可选::有值(值) => 值 == "言",
        可选::空值 => 假,
    };
    令 文本剩余项正确 = 匹配 列表取出末项(文本末项) {
        可选::有值(值) => 值 == "玄",
        可选::空值 => 假,
    };
    令 文本空列表正确 = 匹配 列表取出末项(文本末项) {
        可选::有值(_) => 假,
        可选::空值 => 真,
    };
    如果 非 文本末项正确 或 非 文本剩余项正确 或 非 文本空列表正确 或 非 列表是空(文本末项) {
        返回 假;
    }
    变量 总和 = 0;
    遍历 当前 在 创建待遍历列表() {
        总和 = 总和 + 当前;
    }
    总和 == 42
}
