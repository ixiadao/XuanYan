导入 src::列表::列表;
导入 src::列表::列表创建;
导入 src::列表::列表追加;
导入 src::列表::列表是空;
导入 src::文本与基础值::文本开头是;
导入 src::文本与基础值::文本长度;

公开 宿主 函数 获取当前目录() -> 结果<字符串, 字符串>;
公开 宿主 函数 获取临时目录() -> 结果<字符串, 字符串>;
公开 宿主 函数 创建临时目录() -> 结果<字符串, 字符串>;
公开 宿主 函数 创建临时文件() -> 结果<字符串, 字符串>;
公开 宿主 函数 读取文本文件(路径: 借用<字符串>) -> 结果<字符串, 字符串>;
公开 宿主 函数 读取二进制文件(路径: 借用<字符串>) -> 结果<列表<字节>, 字符串>;
公开 宿主 函数 读取目录(路径: 借用<字符串>) -> 结果<列表<字符串>, 字符串>;
公开 宿主 函数 路径连接(基础: 借用<字符串>, 子路径: 借用<字符串>) -> 字符串;
公开 宿主 函数 路径文件名(路径: 借用<字符串>) -> 结果<字符串, 字符串>;
公开 宿主 函数 路径父目录(路径: 借用<字符串>) -> 结果<字符串, 字符串>;
公开 宿主 函数 路径扩展名(路径: 借用<字符串>) -> 可选<字符串>;
公开 宿主 函数 路径文件主名(路径: 借用<字符串>) -> 可选<字符串>;
公开 宿主 函数 路径是绝对路径(路径: 借用<字符串>) -> 布尔;
公开 宿主 函数 删除文件(路径: 借用<字符串>) -> 结果<空, 字符串>;
公开 宿主 函数 删除目录(路径: 借用<字符串>) -> 结果<空, 字符串>;
公开 宿主 函数 删除目录树(路径: 借用<字符串>) -> 结果<空, 字符串>;
公开 宿主 函数 路径是文件(路径: 借用<字符串>) -> 结果<布尔, 字符串>;
公开 宿主 函数 路径是目录(路径: 借用<字符串>) -> 结果<布尔, 字符串>;
公开 宿主 函数 路径存在(路径: 借用<字符串>) -> 结果<布尔, 字符串>;
公开 宿主 函数 路径规范化(路径: 借用<字符串>) -> 结果<字符串, 字符串>;
公开 宿主 函数 创建目录(路径: 借用<字符串>) -> 结果<空, 字符串>;
公开 宿主 函数 写入文本文件(路径: 借用<字符串>, 内容: 借用<字符串>) -> 结果<空, 字符串>;
公开 宿主 函数 写入二进制文件(路径: 借用<字符串>, 内容: 借用<列表<字节>>) -> 结果<空, 字符串>;
公开 宿主 函数 追加写入二进制文件(路径: 借用<字符串>, 内容: 借用<列表<字节>>) -> 结果<空, 字符串>;
公开 宿主 函数 追加写入文本文件(路径: 借用<字符串>, 内容: 借用<字符串>) -> 结果<空, 字符串>;
公开 宿主 函数 复制文件(源: 借用<字符串>, 目标: 借用<字符串>) -> 结果<空, 字符串>;
公开 宿主 函数 重命名路径(源: 借用<字符串>, 目标: 借用<字符串>) -> 结果<空, 字符串>;
公开 宿主 函数 文件大小(路径: 借用<字符串>) -> 结果<整数, 字符串>;
公开 宿主 函数 文件修改时间(路径: 借用<字符串>) -> 结果<整数, 字符串>;

公开 函数 路径是空目录(路径: 借用<字符串>) -> 结果<布尔, 字符串> {
    令 项目 = 读取目录(路径)?;
    结果::成功(列表是空(项目))
}

公开 函数 读取目录文件(目录: 借用<字符串>) -> 结果<列表<字符串>, 字符串> {
    变量 文件列表: 列表<字符串> = 列表创建();
    遍历 路径 在 读取目录(目录)? {
        令 是文件 = 路径是文件(路径)?;
        如果 非 是文件 {
            继续;
        }
        列表追加(文件列表, 路径);
    }
    结果::成功(文件列表)
}

公开 函数 路径扩展名是(路径: 借用<字符串>, 目标扩展名: 借用<字符串>) -> 布尔 {
    匹配 路径扩展名(路径) {
        可选::有值(扩展名) => 文本长度(扩展名) == 文本长度(目标扩展名) 且 文本开头是(扩展名, 目标扩展名),
        可选::空值 => 假,
    }
}
