公开 结构体 列表<T> {}

公开 函数 列表创建<T>() -> 列表<T> {
    列表<T> {}
}

公开 宿主 函数 列表追加<T>(列表值: 可变借用<列表<T>>, 值: T) -> 空;
公开 宿主 函数 列表长度<T>(列表值: 借用<列表<T>>) -> 整数;
公开 宿主 函数 列表读取文本项(列表值: 借用<列表<字符串>>, 下标: 整数) -> 可选<字符串>;
公开 宿主 函数 列表反转<T>(列表值: 可变借用<列表<T>>) -> 空;
公开 宿主 函数 列表清空<T>(列表值: 可变借用<列表<T>>) -> 空;
公开 宿主 函数 列表取出<T>(列表值: 可变借用<列表<T>>) -> 可选<T>;
公开 宿主 函数 列表取出末项<T>(列表值: 可变借用<列表<T>>) -> 可选<T>;
公开 宿主 函数 列表取出指定项<T>(列表值: 可变借用<列表<T>>, 下标: 整数) -> 可选<T>;

公开 函数 列表是空<T>(列表值: 借用<列表<T>>) -> 布尔 {
    列表长度(列表值) == 0
}

公开 函数 列表连接<T>(左: 列表<T>, 右: 列表<T>) -> 列表<T> {
    变量 结果列表 = 左;
    遍历 值 在 (右) {
        列表追加(结果列表, 值);
    }
    结果列表
}

实现<T> 可遍历<T> 为 列表<T> {
    函数 下一个(可变借用 自身) -> 可选<T> {
        列表取出(自身)
    }
}
