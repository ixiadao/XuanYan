导入 src::文件与路径::读取文本文件;

公开 宿主 函数 写入控制台(值: 借用<字符串>) -> 空;
公开 宿主 函数 写入错误输出(值: 借用<字符串>) -> 空;
公开 宿主 函数 程序参数数量() -> 整数;
公开 宿主 函数 获取程序参数(下标: 整数) -> 可选<字符串>;
公开 宿主 函数 读取环境变量(名称: 借用<字符串>) -> 结果<字符串, 字符串>;
公开 宿主 函数 读取标准输入() -> 结果<字符串, 字符串>;
公开 宿主 函数 读取标准输入行() -> 结果<可选<字符串>, 字符串>;

公开 函数 获取必需程序参数(下标: 整数, 错误: 字符串) -> 结果<字符串, 字符串> {
    匹配 获取程序参数(下标) {
        可选::有值(值) => 结果::成功(值),
        可选::空值 => 结果::失败(错误),
    }
}

公开 函数 读取文本输入(下标: 整数) -> 结果<字符串, 字符串> {
    匹配 获取程序参数(下标) {
        可选::有值(路径) => 读取文本文件(路径),
        可选::空值 => 读取标准输入(),
    }
}
