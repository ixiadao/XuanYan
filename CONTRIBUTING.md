# 贡献玄言

本仓库接受以下公开内容的改进：

- `.xy` 标准库和普通包；
- 可直接运行的示例；
- `editors/vscode` 基础扩展；
- 语言参考、快速开始和其他公开文档；
- 错误复现与使用体验反馈。

编译器和语言服务器的 Rust 实现源码不在本仓库公开，因此本仓库不接受核心实现
代码提交。涉及编译器、诊断、代码生成或 LSP 的问题，请提交可复现的 Issue。

## 提交要求

- 公开名称遵循中文“领域 + 动作或属性”的既有风格。
- 一个提交只解决一个明确问题，不增加兼容别名或重复 API。
- `.xy` 修改应通过当前发布版的格式化检查、包检查和包测试。
- `editors/vscode` 修改应通过 `npm ci`、`npm test` 和 `npm run package`。
- 不提交编译器源码、Cargo 文件、PDB、私钥、令牌、服务器地址、内部路径或构建
  产物。
- 新功能先通过 Issue 说明用途、公开 API 和当前版本边界，再提交变更。

安全问题不要创建公开 Issue，请使用 GitHub `Security` 页面的私密漏洞报告入口。
